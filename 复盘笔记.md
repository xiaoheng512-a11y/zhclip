# zhclip 项目复盘笔记

## 起源（2026-05-24）
用户想找小项目赚钱 → 建议逛 Hacker News Show HN → 发现 **yapsnap**（[CPU-only transcription](https://github.com/kouhxp/yapsnap)，[HN讨论](https://news.ycombinator.com/item?id=48214399)）→ 决定做中文版

## 核心决策
| 决策 | 选型 | 原因 |
|------|------|------|
| 语音模型 | SenseVoice (sherpa-onnx) | 中文优于 Whisper，纯 CPU 可跑 |
| 量化 | INT8 229MB | 完整模型 895MB 需 6.4GB 内存，系统仅 4GB |
| 分块 | 每 30 秒一块 | 避免 OOM（4GB 内存限制） |
| 翻译 | Google Translate (deep-translator) | 免费，无需 API Key（国内可能被墙，已加 8s 超时） |
| 缓存 | 音频 .webm + 结果 .json | 同链接第二次秒出 |
| 前端 | 纯 HTML+JS（单页） | 无依赖，零部署成本 |

## 最终反思（2026-06-07）

**判断：这个方向不该做。**

验证后发现：
- 目标用户（短视频创作者）— 不认识，找不到
- 自己当用户 — 发现通义千问、飞书妙记、沉浸式翻译等免费工具完全覆盖了需求
- 核心场景「离线/本地/隐私」— 人群太小，无法支撑付费产品

**最大教训：先找用户，再做产品。**

> 「不要手里有锤子，看啥都是钉子。」

看到 HN 上的 yapsnap 觉得「这个我能做」就直接动手了，做完了才去想用户在哪。流程应该是反过来的：先找到一个你认识的人在真实场景里遇到的痛点，确认他愿意付钱，再做 MVP。

**收获：**
- 两天从零到上线，技术能力是有的
- sherpa-onnx / SenseVoice / FastAPI / 缓存策略 — 这些技能以后能用上
- 用两周时间和一个周末验证了一个方向，而不是花三个月 — 这是对的节奏

**结论：归档。代码作为作品集保留。下次做产品流程倒过来。**

## 踩过的坑
1. `r"C:\tools\bin\ffmpeg.exe"` 在 Python 三引号字符串里 `\b` → 退格符，路径损坏
2. yt-dlp `--audio-format wav` 需要调用 ffmpeg，但 yt-dlp 子进程找不到 ffmpeg → 改为不转码，让 Python 侧 ffmpeg 处理
3. yt-dlp `--print filename` 输出路径和实际文件后缀不一致（.webm vs .opus）→ 用 `after_move:filename`
4. SenseVoice 全量音频（1200秒）一次性推理需要 6.4GB → 分块 30 秒处理
5. JS 正则 `\\/` 写成双反斜杠 → "Invalid regular expression flags"，整个脚本崩溃
6. 缓存 language="zh" vs 请求 language="auto" 不匹配 → 一直重新转写

## 当前状态（2026-05-24）
- 本地 http://127.0.0.1:8080 ✅
- 公网 https://48111c2dd086de.lhr.life（每次重启变地址） 
- 双语分段 + 时间戳跳转 ✅
- 翻译（国内可能不稳定）✅
- 缓存命中秒出 ✅
- Git 已初始化（master + original 分支）
